<!DOCTYPE html>
<html lang="en">
  <head>
    <style>
      *,
      *:before,
      *::after {
        padding: 0;
        margin: 0;
        box-sizing: border-box;
        font-family: "Poppins" sans-serif;
      }

      body {
        height: 100vh;
        background: linear-gradient(45deg, #8b51ff, #7a37ff);
      }
      form {
        background-color: #ffff;
        width: 450px;
        padding: 50px 40px;
        position: absolute;
        transform: translate(-50%, -50%);
        top: 50%;
        left: 50%;
        border-radius: 8px;
        box-shadow: 20px 20px 30px rgba(0, 0, 0, 0.15);
      }

      label {
        font-weight: 500;
        color: #101030;
      }

      .container {
        background-color: #8b51ff;
        border-radius: 5px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      }

      input {
        display: block;
        width: 100%;
        margin-top: 5px;
        padding: 12px;
        border-radius: 5px;
        outline: none;
        color: #101030;
      }

      #email {
        margin-bottom: 30px;
      }
      input[type="text"] {
        border: 2px solid #c2c2c2;
      }
      input[type="submit"] {
        background-color: #7a37ff;
        border: none;
        color: #ffff;
        font-weight: 500;
        font-size: 18px;
        letter-spacing: 1px;
        cursor: pointer;
        margin-top: 20px;
      }

      p {
        font-size: 14px;
        display: inline-block;
        color: #ffff;
      }

      .background {
        width: 100%;
        height: 100vh;
        background: url("../public/background.png") no-repeat;
        background-size: cover;
        background-position: center;
        filter: blur(10px);
      }
      .forgot {
        padding-bottom: 30px;
      }
    </style>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500&display=swap"
    />
  </head>
  <body>
    <div class="background"></div>
    <form action="/views/forgotPassword" method="post">
      <a href="/views/log" class="go-home">Go Back Home</a>
      <h1 class="forgot">Forgot Password</h1>
      <label for="email">Email</label>
      <input
        type="text"
        name="email"
        id="email"
        placeholder="Enter your email to get a link to reset your password"
      />
      <div class="container">
        <% if (message) { %>
        <p class="message"><%= message %></p>
        <% } %>
      </div>

      <input type="submit" value="Submit" />
    </form>
  </body>
</html>
